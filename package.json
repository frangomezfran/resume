{
  "name": "proyect-resume",
  "version": "1.0.0",
  "description": "My latest resume",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "config": "node ./scripts/generate-config.js",
    "start": "node index.js",
    "scss:watch": "sass --watch src/scss:src/css",
    "scss:build": "sass --verbose --no-source-map src/scss:src/css",
    "dev": "npm run config && concurrently \"npm:start\" \"npm:scss:watch\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "firebase": "^12.3.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "sass": "^1.77.4"
  }
}
